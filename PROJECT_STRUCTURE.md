# Formy Backend 项目结构

## 📁 完整目录树

```
backend/
│
├── app/                                    # 应用主目录
│   ├── __init__.py
│   │
│   ├── main.py                            # FastAPI 应用入口（待创建）
│   │
│   ├── core/                              # ✅ 核心模块
│   │   ├── __init__.py
│   │   ├── config.py                      # ✅ 应用配置
│   │   ├── logging.py                     # 日志配置（待创建）
│   │   └── security.py                    # 安全认证（待创建）
│   │
│   ├── api/                               # API 路由层（待创建）
│   │   ├── __init__.py
│   │   ├── deps.py                        # 依赖注入
│   │   └── v1/
│   │       ├── __init__.py
│   │       ├── routes_tasks.py            # 任务相关接口
│   │       ├── routes_upload.py           # 文件上传接口
│   │       └── routes_auth.py             # 认证接口（可选）
│   │
│   ├── schemas/                           # ✅ 数据传输对象
│   │   ├── __init__.py
│   │   ├── task.py                        # ✅ 任务数据模型
│   │   ├── image.py                       # 图片数据模型（待创建）
│   │   └── user.py                        # 用户数据模型（待创建）
│   │
│   ├── services/                          # ✅ 业务逻辑层
│   │   ├── __init__.py
│   │   │
│   │   ├── tasks/                         # ✅ 任务系统
│   │   │   ├── __init__.py                # ✅
│   │   │   ├── manager.py                 # ✅ 任务管理服务
│   │   │   ├── queue.py                   # ✅ Redis 队列操作
│   │   │   └── worker.py                  # ✅ Worker 工作进程
│   │   │
│   │   ├── storage/                       # 文件存储（待创建）
│   │   │   ├── __init__.py
│   │   │   ├── interface.py               # 存储接口定义
│   │   │   └── local_storage.py           # 本地文件存储实现
│   │   │
│   │   ├── image/                         # 图像处理核心（待创建）
│   │   │   ├── __init__.py
│   │   │   ├── enums.py                   # EditMode 枚举
│   │   │   ├── dto.py                     # EditTaskInput / Result
│   │   │   ├── edit_service.py            # Pipeline 统一入口
│   │   │   │
│   │   │   ├── pipelines/                 # 三大功能流程
│   │   │   │   ├── __init__.py
│   │   │   │   ├── head_swap_pipeline.py   # 换头流程
│   │   │   │   ├── background_pipeline.py  # 换背景流程
│   │   │   │   └── pose_change_pipeline.py # 换姿势流程
│   │   │   │
│   │   │   └── engines/                   # 技术引擎层
│   │   │       ├── __init__.py
│   │   │       ├── base.py                # EngineBase 基类
│   │   │       ├── external_api.py        # 闭源 API 调用
│   │   │       ├── comfyui_engine.py      # ComfyUI 工作流
│   │   │       └── registry.py            # EngineRegistry 配置映射
│   │   │
│   │   └── auth/                          # 认证服务（可选，待创建）
│   │       ├── __init__.py
│   │       ├── jwt.py                     # JWT 生成验证
│   │       └── password.py                # 密码加密
│   │
│   ├── db/                                # 数据库层（可选，待创建）
│   │   ├── __init__.py
│   │   ├── connection.py
│   │   ├── base.py
│   │   └── models.py
│   │
│   ├── utils/                             # ✅ 工具函数
│   │   ├── __init__.py                    # ✅
│   │   ├── id_generator.py                # ✅ ID 生成工具
│   │   └── image_io.py                    # 图片读写工具（待创建）
│   │
│   └── tests/                             # 单元测试（待创建）
│       ├── __init__.py
│       ├── test_tasks.py
│       ├── test_pipelines.py
│       └── test_engines.py
│
├── uploads/                               # 上传文件存储目录（运行时创建）
├── results/                               # 结果文件存储目录（运行时创建）
│
├── engine_config.yml                      # Engine 配置文件（待创建）
├── requirements.txt                       # ✅ Python 依赖
├── .env                                   # 环境变量（需手动创建）
├── CONFIG_EXAMPLE.md                      # ✅ 配置示例
│
├── TASK_SYSTEM_README.md                  # ✅ 任务系统使用文档
├── TASK_SYSTEM_SUMMARY.md                 # ✅ 任务系统实现总结
├── ARCHITECTURE.md                        # ✅ 架构设计文档
├── PROJECT_STRUCTURE.md                   # ✅ 项目结构文档（本文件）
│
└── test_task_system.py                    # ✅ 任务系统测试脚本
```

## 📊 模块状态

| 模块 | 状态 | 文件数 | 说明 |
|------|------|--------|------|
| **schemas/** | ✅ 部分完成 | 1/3 | task.py 已完成 |
| **core/** | ✅ 部分完成 | 1/3 | config.py 已完成 |
| **services/tasks/** | ✅ 完成 | 3/3 | 队列/管理/Worker 已完成 |
| **services/image/** | ❌ 待实现 | 0/8 | Pipeline 和 Engine 层 |
| **services/storage/** | ❌ 待实现 | 0/3 | 文件存储服务 |
| **api/** | ❌ 待实现 | 0/4 | FastAPI 路由 |
| **utils/** | ✅ 部分完成 | 1/2 | id_generator.py 已完成 |
| **tests/** | ❌ 待实现 | 0/3 | 单元测试 |
| **docs/** | ✅ 完成 | 4/4 | 文档齐全 |

## 🎯 各模块职责

### 1. core/ - 核心配置模块
```
config.py       - 应用配置（Redis、存储、JWT 等）
logging.py      - 日志配置（结构化日志、日志级别）
security.py     - 安全相关（JWT 验证、权限控制）
```

### 2. schemas/ - 数据模型层
```
task.py         - 任务相关数据模型
                  • TaskStatus（状态枚举）
                  • EditMode（模式枚举）
                  • TaskInfo（任务信息）
                  • TaskCreateRequest（创建请求）
                  
image.py        - 图片相关数据模型
                  • ImageInfo（图片信息）
                  • UploadResponse（上传响应）
                  
user.py         - 用户相关数据模型（可选）
                  • UserInfo（用户信息）
                  • LoginRequest（登录请求）
```

### 3. services/tasks/ - 任务系统
```
queue.py        - Redis 队列操作
                  • push_task() - 推入队列
                  • pop_task() - 弹出队列
                  • update_task_status() - 更新状态
                  
manager.py      - 任务管理服务
                  • create_task() - 创建任务
                  • get_task() - 查询任务
                  • cancel_task() - 取消任务
                  
worker.py       - Worker 工作进程
                  • 循环消费队列
                  • 分发到 Pipeline
                  • 更新进度和状态
```

### 4. services/image/ - 图像处理核心（待实现）
```
pipelines/      - 业务流程层
  head_swap_pipeline.py      - 换头流程
  background_pipeline.py     - 换背景流程
  pose_change_pipeline.py    - 换姿势流程
  
engines/        - 技术引擎层
  base.py                    - 引擎基类
  external_api.py            - 闭源 API 调用
  comfyui_engine.py          - ComfyUI 工作流
  registry.py                - 引擎注册和配置
```

### 5. services/storage/ - 文件存储（待实现）
```
interface.py         - 存储接口定义（抽象类）
local_storage.py     - 本地文件系统实现
                       • save_file() - 保存文件
                       • get_file() - 读取文件
                       • delete_file() - 删除文件
```

### 6. api/ - API 路由层（待实现）
```
v1/routes_tasks.py   - 任务接口
                       • POST /api/v1/tasks - 创建任务
                       • GET /api/v1/tasks/{task_id} - 查询任务
                       • DELETE /api/v1/tasks/{task_id} - 取消任务
                       
v1/routes_upload.py  - 上传接口
                       • POST /api/v1/upload - 上传图片
                       • GET /api/v1/results/{filename} - 下载结果
                       
v1/routes_auth.py    - 认证接口（可选）
                       • POST /api/v1/auth/login - 登录
                       • POST /api/v1/auth/register - 注册
```

### 7. utils/ - 工具函数
```
id_generator.py      - ID 生成工具
                       • generate_task_id()
                       • generate_file_id()
                       • generate_user_id()
                       
image_io.py          - 图片 I/O 工具
                       • load_image() - 加载图片
                       • save_image() - 保存图片
                       • resize_image() - 调整大小
```

## 🔗 模块依赖关系

```
main.py (FastAPI 应用)
    │
    ├─→ api/v1/routes_tasks.py
    │       │
    │       └─→ services/tasks/manager.py
    │               │
    │               └─→ services/tasks/queue.py
    │                       │
    │                       └─→ Redis
    │
    ├─→ api/v1/routes_upload.py
    │       │
    │       └─→ services/storage/local_storage.py
    │
    └─→ core/config.py


worker.py (Worker 进程)
    │
    ├─→ services/tasks/queue.py
    │       │
    │       └─→ Redis
    │
    └─→ services/image/pipelines/
            │
            └─→ services/image/engines/
                    │
                    └─→ 外部 API / ComfyUI
```

## 📝 文件命名规范

### Python 模块
- 模块名：小写 + 下划线（`task_manager.py`）
- 类名：大驼峰（`TaskService`）
- 函数名：小写 + 下划线（`create_task()`）
- 常量名：大写 + 下划线（`REDIS_HOST`）

### 目录结构
- 功能模块：`services/tasks/`
- 数据模型：`schemas/`
- API 路由：`api/v1/`
- 工具函数：`utils/`

### 文档文件
- 大写 + 下划线：`TASK_SYSTEM_README.md`
- 配置示例：`CONFIG_EXAMPLE.md`
- 架构文档：`ARCHITECTURE.md`

## 🚀 快速导航

### 开发新功能时的文件定位

#### 创建新的任务类型
1. `schemas/task.py` - 添加枚举值到 `EditMode`
2. `services/tasks/worker.py` - 添加分发逻辑
3. `services/image/pipelines/` - 创建新的 Pipeline

#### 添加新的 API 接口
1. `api/v1/routes_*.py` - 创建路由文件
2. `schemas/*.py` - 定义请求/响应模型
3. `services/*` - 实现业务逻辑

#### 对接新的 AI 引擎
1. `services/image/engines/` - 创建新的 Engine
2. `engine_config.yml` - 添加引擎配置
3. `services/image/engines/registry.py` - 注册引擎

#### 添加新的存储方式
1. `services/storage/` - 实现 Storage 接口
2. `core/config.py` - 添加配置项

## 📦 依赖包说明

### 核心依赖
```
fastapi         - Web 框架
uvicorn         - ASGI 服务器
pydantic        - 数据验证
redis           - Redis 客户端
```

### 工具依赖
```
python-dotenv   - 环境变量
aiofiles        - 异步文件操作
python-multipart - 文件上传
```

### 可选依赖
```
sqlalchemy      - 数据库 ORM（如需数据库）
pillow          - 图片处理
requests        - HTTP 请求（调用外部 API）
websockets      - WebSocket 支持
```

## 🔧 开发工具推荐

### 代码质量
```
black           - 代码格式化
flake8          - 代码检查
mypy            - 类型检查
pytest          - 单元测试
```

### 开发辅助
```
ipython         - 交互式 Shell
python-dotenv   - 环境变量管理
```

## 📊 代码统计（当前）

```
✅ 已完成:
- Python 文件: 9 个
- 代码行数: ~1500 行
- 文档文件: 5 个
- 文档行数: ~2000 行

❌ 待实现:
- Python 文件: ~20 个
- 估计代码: ~3000 行
```

## 🎯 开发优先级

### P0 - 核心功能（1-2周）
- [ ] FastAPI 主入口（`main.py`）
- [ ] API 路由层（`api/v1/`）
- [ ] 文件上传和存储（`services/storage/`）
- [ ] Pipeline 层骨架（`services/image/pipelines/`）

### P1 - 核心能力（2-3周）
- [ ] Engine 层实现（`services/image/engines/`）
- [ ] 对接闭源 API
- [ ] 对接 ComfyUI
- [ ] 图片处理工具（`utils/image_io.py`）

### P2 - 完善功能（1-2周）
- [ ] 认证系统（`services/auth/`）
- [ ] 日志系统（`core/logging.py`）
- [ ] 单元测试（`tests/`）
- [ ] 性能优化

### P3 - 扩展功能（可选）
- [ ] WebSocket 实时推送
- [ ] 数据库持久化
- [ ] 任务重试机制
- [ ] 监控和告警

---

**文档版本**: v1.0  
**更新时间**: 2025-11-17  
**维护者**: Formy 开发团队

